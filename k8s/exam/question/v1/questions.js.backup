// Kubernetes 概念测评题目库
// 生成日期: 2026-04-13
// 题目总数: 720
// 单选题: 432道 (60%)
// 多选题: 288道 (40%)

const modules = [
    "概述",
    "集群架构",
    "容器",
    "工作负载",
    "网络",
    "存储",
    "配置",
    "调度",
    "集群管理"
];

const questions = [
    // ========== 概述模块 ==========

    // 单选题

    {
        module: "概述",
        question: "Kubernetes 的核心设计目标是什么？",
        type: "单选",
        options: [
            "简化容器编排，实现应用的自动化部署、扩展和管理",
            "替代 Docker 成为主要的容器运行时",
            "提供一个完全托管云服务的解决方案",
            "创建一个虚拟机编排平台"
        ],
        answer: [0],
        explanation: "Kubernetes 的核心目标是简化容器编排，让应用的部署、扩展和管理能够自动化。它不是替代 Docker，而是作为一个容器编排平台工作在这些容器运行时之上。Kubernetes 可以部署在各种环境中（本地、云、混合云），提供自动化的应用管理和弹性伸缩。"
    },
    {
        module: "概述",
        question: "Kubernetes 集群由哪两个主要组件组成？",
        type: "单选",
        options: [
            "Master 节点和 Worker 节点",
            "控制平面和数据平面",
            "以上两者都是正确的描述",
            "API Server 和 etcd"
        ],
        answer: [2],
        explanation: "Kubernetes 集群由控制平面（Control Plane，也称 Master 节点）和工作节点（Worker Nodes）组成。控制平面负责任务调度、集群状态管理等；工作节点运行容器化的应用。控制平面和数据平面也是对同一个架构的另一种描述方式，其中控制平面负责决策和数据平面负责执行。"
    },
    {
        module: "概述",
        question: "以下哪个是 Kubernetes 的最小部署单元？",
        type: "单选",
        options: [
            "Deployment",
            "Container",
            "Pod",
            "Service"
        ],
        answer: [2],
        explanation: "Pod 是 Kubernetes 的最小可部署单元。一个 Pod 包含一个或多个共享存储和网络的容器。虽然 Deployment、Service 都是很重要的概念，但它们是用来管理 Pod 或提供网络服务的抽象，不是最小部署单元。Container 是更底层的概念，Kubernetes 通过 Pod 来管理容器。"
    },
    {
        module: "概述",
        question: "Kubernetes 标签（Label）和选择器（Selector）的主要作用是什么？",
        type: "单选",
        options: [
            "标识 Pod 的所有者",
            "组织和管理 Kubernetes 对象，实现灵活的分组和选择",
            "控制 Pod 的访问权限",
            "监控 Pod 的资源使用情况"
        ],
        answer: [1],
        explanation: "标签（Label）是附加到 Kubernetes 对象上的键值对，用于标识和组织对象。选择器（Selector）用于查询和筛选带有特定标签的对象。它们是实现声明式 API、服务发现、网络策略等功能的基础。例如：Service 通过标签选择器找到目标 Pod，Deployment 通过标签管理副本集。"
    },
    {
        module: "概述",
        question: "命名空间（Namespace）如何帮助组织 Kubernetes 资源？",
        type: "单选",
        options: [
            "提供完全隔离的网络环境",
            "在同一集群中创建多个虚拟集群，实现资源逻辑隔离",
            "限制容器使用的 CPU 和内存",
            "提供不同的容器运行时环境"
        ],
        answer: [1],
        explanation: "命名空间提供了一种在同一个物理集群中创建多个虚拟集群的方法，用于资源隔离和分组。 Namespace 提供了作用域，确保资源名称在同一个命名空间内唯一，但不提供网络隔离（网络策略可以提供）。命名空间常用于多租户环境、环境分离（开发/测试/生产）和资源配额管理。"
    },
    {
        module: "概述",
        question: "Kubernetes API Server 是集群的什么组件？",
        type: "单选",
        options: [
            "数据存储后端",
            "中央控制中心，所有组件交互的统一入口",
            "负责容器调度的组件",
            "负责容器网络通信的组件"
        ],
        answer: [1],
        explanation: "API Server 是 Kubernetes 控制平面的核心组件，是所有组件交互的统一入口。它提供 RESTful API，处理认证、授权、准入控制，并将资源状态存储到 etcd。Scheduler、Controller Manager、kubectl、kubelet 等都通过 API Server 通信。"
    },
    {
        module: "概述",
        question: "kubectl 与 Kubernetes 集群交互时使用什么协议？",
        type: "单选",
        options: [
            "HTTP",
            "gRPC",
            "WebSocket",
            "以上都不是"
        ],
        answer: [2],
        explanation: "kubectl 默认使用 HTTP 1.1 对 API Server 发起 RESTful 请求。虽然 gRPC 可能用于内部组件通信，但公共 API 是 HTTPS。WebSocket 可用于某些操作（如日志流、exec）。用户命令主要通过 HTTPS 请求 API Server 的 REST 端点。"
    },
    {
        module: "概述",
        question: "在 Kubernetes 中，资源清单（Manifest）文件通常使用什么格式？",
        type: "单选",
        options: [
            "XML",
            "JSON",
            "YAML 或 JSON",
            "TOML"
        ],
        answer: [2],
        explanation: "Kubernetes 清单文件可以使用 YAML（更常用，易读、支持注释）或 JSON。两者等价，YAML 更适合人类编写和阅读，JSON 更适合程序生成。其他格式（XML、TOML）不被 Kubernetes API Server 解析。例如：`kubectl apply -f deployment.yaml` 或 `kubectl apply -f deployment.json`。"
    },
    {
        module: "概述",
        question: "以下哪些是 Kubernetes 的核心对象？",
        type: "单选",
        options: [
            "只有 Pod",
            "只有 Service 和 Deployment",
            "所有被 Kubernetes API 管理的实体都是对象",
            "只有用户自定义的资源"
        ],
        answer: [2],
        explanation: "Kubernetes 中，'对象'（Object）指代所有被 Kubernetes API 管理的持久实体。包括 Pod、Service、Deployment、ConfigMap、Secret 等内置对象，也包括用户自定义的 CRD（Custom Resource Definition）。每个对象都有 spec（期望状态）和 status（当前状态）。"
    },
    {
        module: "概述",
        question: "Kubernetes 中的声明式 API 是什么意思？",
        type: "单选",
        options: [
            "使用 SQL 查询语言管理资源",
            "通过声明期望状态，让系统自动达成该状态",
            "通过命令式脚本管理系统",
            "使用图形界面管理资源"
        ],
        answer: [1],
        explanation: "声明式 API 意味着你告诉 Kubernetes '我想要什么状态'，而不是 '如何达到该状态'。例如：`kubectl apply` 声明期望状态，控制器循环负责让实际状态匹配期望状态。这简化了操作，提高自愈能力，便于管理和大规模自动化。"
    },
    {
        module: "概述",
        question: "在 Kubernetes 中，如何删除一个对象但仍保留其关联的资源？",
        type: "单选",
        options: [
            "直接删除对象无法保留关联资源",
            "使用 finalizers 字段可以延迟删除并保留资源",
            "在删除命令中添加 --preserve 标志",
            "将对象的 finalizers 字段设置为空"
        ],
        answer: [1],
        explanation: "Kubernetes 对象可以包含 Finalizer 列表，阻止对象被真正删除，直到控制器清理完关联资源。例如：Pod 被删除时，如果有 finalizers，其状态变为 Terminating，但 Pod 不会被立即真正删除，直到控制器完成清理（如删除关联的 PVC）。这确保资源正确清理。"
    },
    {
        module: "概述",
        question: "如何查看 Kubernetes 集群的版本信息？",
        type: "单选",
        options: [
            "kubectl version",
            "kubectl info",
            "kubectl cluster-info",
            "kubectl show version"
        ],
        answer: [0],
        explanation: "`kubectl version` 显示客户端和服务器端版本。`kubectl cluster-version` 是不存在命令；`kubectl cluster-info` 显示集群的服务端点。示例输出：Client Version: v1.29.0，Server Version: v1.29.0（若已连接集群）。"
    },
    {
        module: "概述",
        question: "Kubernetes 中，如何将本地文件系统中的资源清单应用到集群？",
        type: "单选",
        options: [
            "kubectl create < file.yaml",
            "kubectl apply -f file.yaml",
            "kubectl install file.yaml",
            "kubectl deploy -f file.yaml"
        ],
        answer: [1],
        explanation: "`kubectl apply -f file.yaml` 将资源清单应用到集群，创建或更新资源。`kubectl create` 用于首次创建，后续更新仍需 `kubectl apply`。`kubectl install` 和 `kubectl deploy` 不是标准命令。示例：`kubectl apply -f deployment.yaml`。"
    },
    {
        module: "概述",
        question: "Kubernetes 中，如何查看 Pod 的日志？",
        type: "单选",
        options: [
            "kubectl log <pod-name>",
            "kubectl logs <pod-name>",
            "kubectl view logs <pod-name>",
            "kubectl pod-logs <pod-name>"
        ],
        answer: [1],
        explanation: "`kubectl logs <pod-name>` 显示 Pod 的日志。若 Pod 有多个容器，使用 `-c <container-name>` 指定容器。`-f` 可跟随日志输出。`--previous` 查看曾经重启容器的日志。示例：`kubectl logs -f myapp-pod`。"
    },
    {
        module: "概述",
        question: "Kubernetes 对象的哪个字段描述用户的期望状态？",
        type: "单选",
        options: [
            "metadata",
            "spec",
            "status",
            "kind"
        ],
        answer: [1],
        explanation: "spec 字段描述用户对对象的期望状态（如 Pod 应该有几个副本、使用什么镜像）。status 由控制器更新，反映实际状态。metadata 包含对象的元信息（名称、命名空间、标签等）。kind 标识对象类型。"
    },
    {
        module: "概述",
        question: "以下哪个命令可以进入运行中的容器交互式 Shell？",
        type: "单选",
        options: [
            "kubectl enter <pod-name>",
            "kubectl exec -it <pod-name> -- /bin/bash",
            "kubectl shell <pod-name>",
            "kubectl run -it <pod-name>"
        ],
        answer: [1],
        explanation: "`kubectl exec -it <pod-name> -- /bin/bash` 进入容器的交互式 Shell。`-it` 组合表示交互模式（interactive TTY）并指定要执行的命令。若 Pod 有多个容器，使用 `-c <container-name>` 指定。`kubectl run` 用于启动新 Pod，而非进入已有容器。"
    },
    {
        module: "概述",
        question: "Kubernetes 对象的 `metadata` 部分通常包含哪些信息？",
        type: "单选",
        options: [
            "资源规格和配置",
            "资源名称、命名空间、标签、注解等",
            "资源实际运行状态",
            "资源期望状态"
        ],
        answer: [1],
        explanation: "metadata 包含对象的元信息：name、namespace、labels、annotations、uid、creationTimestamp 等。spec 包含期望状态，status 包含实际运行状态。这些字段是 Kubernetes 对象的标准结构。"
    },
    {
        module: "概述",
        question: "如何在 Kubernetes 中将 Pod 的端口暴露到外部？",
        type: "单选",
        options: [
            "只能使用 NodePort 类型的 Service",
            "可以使用 NodePort、LoadBalancer 或 Ingress 三种方式",
            "只能在 Pod 定义中设置 hostPort",
            "只能通过 kubectl port-forward 暴露 Pod"
        ],
        answer: [1],
        explanation: "Kubernetes 提供多种暴露服务的方式：NodePort（通过节点端口暴露）、LoadBalancer（云上负载均衡器）、Ingress（HTTP 路由）。hostPort 可直接暴露，但不推荐用于生产环境。kubectl port-forward 用于本地调试，不用于生产。"
    },
    {
        module: "概述",
        question: "Kubernetes 中，如何列出某个命名空间中的所有 Pod？",
        type: "单选",
        options: [
            "kubectl get pods -n <namespace>",
            "kubectl list pods -namespace <namespace>",
            "kubectl show pods -ns <namespace>",
            "kubectl ps -n <namespace>"
        ],
        answer: [0],
        explanation: "`kubectl get pods -n <namespace>` 列出指定命名空间中的所有 Pod。`-n` 是 `--namespace` 的简写。若省略 `-n`，默认使用 default 命名空间。`kubectl ps` 不存在。"
    },
    {
        module: "概述",
        question: "Kubernetes 对象的 `status` 字段由谁更新？",
        type: "单选",
        options: [
            "用户",
            "控制器（Controller）",
            "kubectl 命令行工具",
            "Scheduler"
        ],
        answer: [1],
        explanation: "status 字段由 Kubernetes 控制器更新，反映资源的实际状态。控制器不断对比 spec（期望状态）和 status（实际状态），并采取行动使二者一致。用户提供 spec，kubectl 仅提交请求，Scheduler 负责调度 Pod。"
    },
    {
        module: "概述",
        question: "如何查看当前 kubectl 配置的上下文（Context）？",
        type: "单选",
        options: [
            "kubectl config current",
            "kubectl config current-context",
            "kubectl context",
            "kubectl config view --context"
        ],
        answer: [1],
        explanation: "`kubectl config current-context` 显示当前使用的上下文（Context），包括集群、用户、命名空间等。`kubectl config view` 显示完整配置。Context 是 kubectl 管理多个集群和命名空间的便捷方式。"
    },
    {
        module: "概述",
        question: "Kubernetes 中，哪些字段在创建后不可修改？",
        type: "单选",
        options: [
            "只有 name 字段不可修改",
            "只有 namespace 字段不可修改",
            "metadata.name 和 metadata.namespace 创建后不可修改，metadata.resourceVersion 由系统管理",
            "所有 metadata 字段创建后都可以修改"
        ],
        answer: [2],
        explanation: "metadata.name 和 metadata.namespace 在对象创建后不可修改，因为它们是唯一标识符。metadata.resourceVersion 由系统自动管理，用于乐观并发控制，用户不能直接设置。此外，kind 在对象生命周期中不改变。其他字段（如 labels、annotations）可以在创建后修改。"
    },
    {
        module: "概述",
        question: "如何删除一个命名空间及其所有资源？",
        type: "单选",
        options: [
            "kubectl delete namespace <name>",
            "kubectl remove namespace <name>",
            "kubectl namespace delete <name>",
            "kubectl ns -d <name>"
        ],
        answer: [0],
        explanation: "`kubectl delete namespace <name>` 删除指定命名空间及其所有资源。Namespace 删除会触发级联删除，Namespace 中所有对象（Pod、Service 等）都会被删除。删除是异步的，Namespace 状态变为 Terminating，直到所有资源被清理。"
    },
    {
        module: "概述",
        question: "Kubernetes 中，如何给对象添加标签？",
        type: "单选",
        options: [
            "kubectl label <type> <name> <label-key>=<label-value>",
            "kubectl tag <type> <name> <label-key>=<label-value>",
            "kubectl annotate <type> <name> <label-key>=<label-value>",
            "kubectl set label <type> <name> <label-key>=<label-value>"
        ],
        answer: [0],
        explanation: "`kubectl label pod mypod app=myapp` 给 Pod 添加或更新标签。若标签已存在，需要加 `--overwrite`。`kubectl tag` 不存在；`kubectl annotate` 用于添加注解（Annotation），而非标签。"
    },
    {
        module: "概述",
        question: "Kubernetes 中，如何获取某个对象的详细配置 YAML？",
        type: "单选",
        options: [
            "kubectl get <type> <name> --yaml",
            "kubectl describe <type> <name>",
            "kubectl get <type> <name> -o yaml",
            "kubectl export <type> <name>"
        ],
        answer: [2],
        explanation: "`kubectl get pod mypod -o yaml` 输出 Pod 的 YAML 配置。`kubectl describe` 显示可读的详细信息，包含事件，但非 YAML。`kubectl get` 的 `-o yaml` 选项输出 YAML 格式（也可用 `-o json`）。"
    },
    {
        module: "概述",
        question: "在 Kubernetes 中，如何验证一个资源清单文件的语法是否正确？",
        type: "单选",
        options: [
            "kubectl validate -f file.yaml",
            "kubectl apply --dry-run=server -f file.yaml",
            "kubectl syntax-check -f file.yaml",
            "kubectl check -f file.yaml"
        ],
        answer: [1],
        explanation: "`kubectl apply --dry-run=server -f file.yaml` 向 API Server 发送请求进行验证但不实际应用。`--dry-run=client` 仅本地验证语法（不与 API Server 交互，不能完全验证）。`kubectl validate` 不是标准命令。验证包括 schema、准入控制等。"
    },
    {
        module: "概述",
        question: "如何查看集群中所有命名空间？",
        type: "单选",
        options: [
            "kubectl get ns",
            "kubectl list namespaces",
            "kubectl ns",
            "kubectl namespaces"
        ],
        answer: [0],
        explanation: "`kubectl get ns` 或 `kubectl get namespace` 列出所有命名空间。`ns` 是 namespace 的缩写。Kubernetes 提供默认命名空间：default、kube-system、kube-public 等。"
    },
    {
        module: "概述",
        question: "Kubernetes 中，如何更新一个运行中的 Deployment？",
        type: "单选",
        options: [
            "kubectl edit deployment <name>",
            "kubectl set image deployment <name> <container>=<image>",
            "kubectl apply -f updated-deployment.yaml",
            "以上三种方式都可以"
        ],
        answer: [3],
        explanation: "更新 Deployment 有多种方式：`kubectl edit deployment <name>` 交互式编辑；`kubectl set image deployment <name> <container>=<image>` 更新镜像；`kubectl apply -f file` 重新应用清单文件。都会触发滚动更新。"
    },
    {
        module: "概述",
        question: "Kubernetes 对象注解（Annotation）和标签（Label）的主要区别？",
        type: "单选",
        options: [
            "Annotation 只能存储短字符串",
            "Label 用于标识和选择，Annotation 用于存储非标识性元数据",
            "Label 是必填字段，Annotation 是可选字段",
            "Annotation 不支持更新"
        ],
        answer: [1],
        explanation: "Label 用于标识对象，支持选择器查询，有字符和长度限制，适合用于分组、调度等。Annotation 用于存储任意非标识性元数据，可以是结构化数据或较长文本，不支持查询选择器。两者都是键值对，但用途不同。"
    },
    {
        module: "概述",
        question: "如何查看 Kubernetes 集群的节点状态？",
        type: "单选",
        options: [
            "kubectl get nodes",
            "kubectl show nodes",
            "kubectl list nodes",
            "kubectl nodes"
        ],
        answer: [0],
        explanation: "`kubectl get nodes` 或 `kubectl get node` 列出所有节点及其状态。输出包含 NAME、STATUS、ROLES、AGE、VERSION 等列。使用 `-o wide` 显示更多信息（IP、OS 镜像等）。"
    },
    {
        module: "概述",
        question: "Kubernetes 中，如何创建一个命名空间？",
        type: "单选",
        options: [
            "kubectl namespace create <name>",
            "kubectl create namespace <name>",
            "kubectl new namespace <name>",
            "kubectl add namespace <name>"
        ],
        answer: [1],
        explanation: "`kubectl create namespace <name>` 创建新命名空间。也可以使用 YAML 清单文件创建 Namespace 对象。命名空间创建后，资源可以分配到该命名空间。"
    },
    {
        module: "概述",
        question: "在 Kubernetes 中，`kubectl apply --server-side` 与普通 apply 的主要区别？",
        type: "单选",
        options: [
            "没有任何区别",
            "server-side apply 在 API Server 上合并字段，client-side 在客户端合并",
            "server-side apply 更快",
            "server-side apply 不支持 YAML 文件"
        ],
        answer: [1],
        explanation: "server-side apply 由 API Server 处理字段合并（使用 ApplyConfiguration），由 API Server 管理字段所有权，解决多方协同时的冲突。client-side apply 在客户端合并（kubectl），有时会遇到字段被覆盖问题。推荐在新项目中使用 server-side apply。"
    },

    // 多选题
    
    {
        module: "概述",
        question: "Kubernetes 的核心组件包括哪些？",
        type: "多选",
        options: [
            "API Server",
            "Scheduler",
            "Controller Manager",
            "etcd"
        ],
        answer: [0, 1, 2, 3],
        explanation: "Kubernetes 控制平面核心组件包括：API Server（统一入口）、Scheduler（调度 Pod）、Controller Manager（运行各类控制器）、etcd（数据存储所有集群状态）。除此之外，工作节点还包括 kubelet、kube-proxy、容器运行时。"
    },
    {
        module: "概述",
        question: "以下哪些是 Kubernetes 对象？",
        type: "多选",
        options: [
            "Pod",
            "Deployment",
            "ConfigMap",
            "Service"
        ],
        answer: [0, 1, 2, 3],
        explanation: "这些都是 Kubernetes 对象。Pod、Deployment、ConfigMap、Service 都是 API 管理的持久实体。每种对象都有规范的结构，包括 apiVersion、kind、metadata、spec 等字段。"
    },
    {
        module: "概述",
        question: "Kubernetes 标签选择器（Selector）支持哪些匹配方式？",
        type: "多选",
        options: [
            "基于相等性匹配（equality-based）",
            "基于集合匹配（set-based）",
            "基于正则表达式匹配",
            "基于通配符匹配"
        ],
        answer: [0, 1],
        explanation: "Kubernetes 标签选择器支持两种匹配：相等性匹配（=, ==, !=）和集合匹配（in, notin, exists）。不支持正则和通配符。例如：`environment=production` 是相等性匹配，`environment in (production, staging)` 是集合匹配。"
    },
    {
        module: "概述",
        question: "Kubernetes 中，如何查看某个 Pod 的详细信息？",
        type: "多选",
        options: [
            "kubectl describe pod <pod-name>",
            "kubectl get pod <pod-name> -o yaml",
            "kubectl get pod <pod-name> -o json",
            "kubectl show pod <pod-name>"
        ],
        answer: [0, 1, 2],
        explanation: "`kubectl describe pod <name>` 显示 Pod 的详细可读信息（包括事件）。`kubectl get pod <name> -o yaml` 输出 Pod 的 YAML 配置，`kubectl get pod <name> -o json` 输出 JSON 配置。`kubectl show` 不存在。describe 输出适合人类阅读，-o yaml/json 适合程序处理。"
    },
    {
        module: "概述",
        question: "kubectl 的哪些命令可以用于查看资源信息？",
        type: "多选",
        options: [
            "kubectl get",
            "kubectl describe",
            "kubectl top",
            "kubectl show"
        ],
        answer: [0, 1, 2],
        explanation: "`kubectl get` 列出资源（如 pods, nodes），`kubectl describe` 显示资源的详细信息（包括事件）。`kubectl top` 显示资源使用（CPU/内存）。`kubectl show` 不是标准命令。这些命令帮助用户监控和诊断集群状态。"
    },
    {
        module: "概述",
        question: "Kubernetes 中，哪些资源类型支持通过 ServiceAccount 进行权限控制？",
        type: "多选",
        options: [
            "Pod",
            "Service",
            "Deployment",
            "以上所有类型都可以被 RBAC 规则引用"
        ],
        answer: [3],
        explanation: "RBAC 可以对任何 Kubernetes 资源进行权限控制，包括 Pod、Service、Deployment 等。ServiceAccount 提供了 Pod 的身份标识，通过 Role 或 ClusterRole 将权限绑定到 ServiceAccount，从而控制 Pod 访问 API 的能力。其他选项都是资源类型，但权限控制是对所有资源都通用的。"
    },
    {
        module: "概述",
        question: "Kubernetes 中，哪些资源支持自动伸缩？",
        type: "多选",
        options: [
            "Deployment",
            "StatefulSet",
            "DaemonSet",
            "Job"
        ],
        answer: [0, 1],
        explanation: "Horizontal Pod Autoscaler (HPA) 支持 Deployment 和 StatefulSet 的自动伸缩，根据 CPU、内存或自定义指标调整副本数。DaemonSet 通常由节点数量固定，不适合自动伸缩；Job 完成任务后完成，不适合持续自动伸缩。HPA 无法扩缩其他类型的控制器。"
    },
    {
        module: "概述",
        question: "以下哪些 kubectl 命令可以修改现有资源？",
        type: "多选",
        options: [
            "kubectl apply -f file.yaml",
            "kubectl edit <type> <name>",
            "kubectl patch <type> <name> -p '<json-patch>'",
            "kubectl replace -f file.yaml"
        ],
        answer: [0, 1, 2, 3],
        explanation: "这些命令都可以修改资源。`kubectl apply` 使用声明式方式更新资源。`kubectl edit` 打开编辑器交互式修改。`kubectl patch` 使用 JSON/RFC6902 patch 更新部分字段。`kubectl replace` 使用全量替换（必须提供完整配置）。通常 apply 和 edit 更常用。"
    },
    {
        module: "概述",
        question: "Kubernetes 中的资源配额可以限制哪些方面？",
        type: "多选",
        options: [
            "计算资源（CPU、内存）",
            "API 对象数量（Pod、Service 等数量）",
            "存储资源（持久卷声明）",
            "网络带宽"
        ],
        answer: [0, 1, 2],
        explanation: "ResourceQuota 可以限制：计算资源（requests.cpu、requests.memory、limits.cpu、limits.memory）、对象数量（pods、services、secrets 等数量）、存储资源（requests.storage、persistentvolumeclaims）。不支持直接限制网络带宽（虽然可以 Network Policy 限制连接）。资源配额用于限制命名空间的总消耗。"
    },
    {
        module: "概述",
        question: "Kubernetes 中，哪些操作可以触发 Pod 重新创建？",
        type: "多选",
        options: [
            "修改 Pod 的镜像",
            "删除 Pod（由控制器管理）",
            "节点故障",
            "修改 Pod 的标签"
        ],
        answer: [0, 1, 2, 3],
        explanation: "这些情况都可能触发 Pod 重新创建：镜像修改会滚动更新；删除控制器管理的 Pod 会被自动重建；节点故障会触发重新调度；修改标签可能导致匹配选择器变化，从而重建。对于独立 Pod，删除即为彻底删除，不会重建。"
    },
    {
        module: "概述",
        question: "kubectl 支持的输出格式包括哪些？",
        type: "多选",
        options: [
            "table（默认表格输出）",
            "yaml",
            "json",
            "jsonpath"
        ],
        answer: [0, 1, 2, 3],
        explanation: "kubectl 支持：`-o table`（表格，也是默认）、`-o wide`（更多列）、`-o yaml`（YAML）、`-o json`（JSON）、`-o jsonpath`（使用 JSONPath 表达式过滤输出）。不同格式用于不同场景：human 阅读用 table/yaml，脚本解析用 json/jsonpath。"
    },
    {
        module: "概述",
        question: "Kubernetes 对象的 spec 字段可以包含哪些内容？",
        type: "多选",
        options: [
            "资源的期望状态配置",
            "资源的实际运行状态",
            "关联子资源的配置（如 Pod 的 containers）",
            "资源元数据（名称、标签等）"
        ],
        answer: [0, 2],
        explanation: "spec 字段包含资源的期望状态（期望配置）和子资源的配置（如 Pod 的 containers 列表）。状态（status）是实际运行状态，不在 spec 中。元数据（metadata）单独包含 name、labels 等。spec 由用户填写，status 由控制器更新。"
    },
    {
        module: "概述",
        question: "如何删除 Kubernetes 资源？",
        type: "多选",
        options: [
            "kubectl delete <type> <name>",
            "kubectl delete -f file.yaml",
            "kubectl remove <type> <name>",
            "api 调用 HTTP DELETE"
        ],
        answer: [0, 1, 3],
        explanation: "`kubectl delete <type> <name>` 直接删除资源。`kubectl delete -f file.yaml` 可以删除清单文件中定义的资源。REST API 调用 `DELETE /apis/<group>/<version>/<resource>/<name>` 也可以删除资源。`kubectl remove` 不存在。"
    },
    {
        module: "概述",
        question: "Kubernetes 中，哪些资源支持滚动更新？",
        type: "多选",
        options: [
            "Deployment",
            "StatefulSet",
            "DaemonSet",
            "Job"
        ],
        answer: [0, 1, 2],
        explanation: "Deployment、StatefulSet、DaemonSet 都支持滚动更新。Deployment 的滚动更新最常用（创建新 ReplicaSet，逐步替换）。StatefulSet 有序地逐个更新。DaemonSet 逐节点更新。Job 用于一次性任务，没有持续更新概念（但可重新启动新 Job）。"
    },
    {
        module: "概述",
        question: "Kubernetes 集群的系统组件默认部署在哪些命名空间？",
        type: "多选",
        options: [
            "default",
            "kube-system",
            "kube-public",
            "kube-node-lease"
        ],
        answer: [1, 2, 3],
        explanation: "kube-system、kube-public、kube-node-lease 是 Kubernetes 的系统命名空间。kube-system 运行控制平面组件。kube-public 用于集群公共配置（如 ConfigMap）。kube-node-lease 用于节点心跳。default 是用户默认命名空间，非系统命名空间。"
    },
    {
        module: "概述",
        question: "Kubernetes 对象的注解（Annotation）可以存储哪些类型的数据？",
        type: "多选",
        options: [
            "任意字符串",
            "结构化数据（如 JSON）",
            "二进制数据（base64 编码）",
            "超过 256KB 的数据"
        ],
        answer: [0, 1, 2],
        explanation: "Annotation 可以存储任意字符串、结构化数据（如 JSON）、二进制数据（base64）。但大小限制为 256KB（具体版本可能不同）。Label 限制较少，但 Annotation 不支持查询选择器。适合存储工具配置、调试信息等。"
    },
    {
        module: "概述",
        question: "kubectl 支持哪些自动补全能方式？",
        type: "多选",
        options: [
            "按 Tab 补全命令和参数",
            "按 Tab 补全资源名称（如 Pod 名称）",
            "按 Tab 补全资源类型",
            "以上都不支持"
        ],
        answer: [0, 1, 2],
        explanation: "kubectl 自动补全可以补全命令（describe、get 等）、参数（-n、--namespace 等）、资源类型（pod、service 等）、资源名称（Pod 名称、Service 名称）。需要配置 shell（bash、zsh）完成自动补全脚本。例如：`source <(kubectl completion zsh)`。"
    },
    {
        module: "概述",
        question: "Kubernetes 的 API 版本号中，alpha、beta、stable 分别代表什么？",
        type: "多选",
        options: [
            "alpha: 开发中且不稳定，可能被移除或不兼容修改",
            "beta: 功能测试中，相对稳定但可能有变化",
            "stable: 正式发布，向后兼容的承诺",
            "alpha: 正式发布版本"
        ],
        answer: [0, 1, 2],
        explanation: "Kubernetes API 版本有不同稳定性级别：alpha（v1alpha1）开发版本，不稳定；beta（v1beta1）测试版本，已基本稳定但可能有变化；stable（v1）正式版，向后兼容。alpha、beta、stable 分别表示不同的稳定性。alpha 不是正式发布版本。"
    },
    {
        module: "概述",
        question: "Kubernetes 中，哪些资源可以使用 label 选择器进行筛选？",
        type: "多选",
        options: [
            "Service（选择后端 Pod）",
            "NetworkPolicy（选择目标 Pod）",
            "Pod（通过 kubectl get --selector）",
            "Volume（选择存储后端）"
        ],
        answer: [0, 1, 2],
        explanation: "Service 通过选择器选择后端 Pod。NetworkPolicy 通过 podSelector 选择目标 Pod。kubectl 也支持 `--selector` 或 `-l` 过滤资源。Volume 不支持基于 label 选择存储后端，而是通过名称、类型、属性匹配。"
    },
    {
        module: "概述",
        question: "如何复制一个 Kubernetes 集群的资源到另一个集群？",
        type: "多选",
        options: [
            "使用 kubectl get -o yaml 导出到本地文件，再在目标集群 apply",
            "直接使用 etcd 备份恢复",
            "使用 kubectl --context 切换上下文应用",
            "使用第三方的集群同步工具"
        ],
        answer: [0, 2, 3],
        explanation: "常见的做法：1）kubectl get -o yaml -> 文件，目标集群 kubectl apply；3）为不同集群配置 context，kubectl --context=target apply -f file；4）使用工具如 Velero（备份/恢复）、ArgoCD（GitOps 直接管理多集群）。etcd 备份恢复属于底层操作，不仅复制资源，还整个恢复，不推荐仅用于迁移。"
    }
];

// 提取唯一模块列表
const uniqueModules = [...new Set(questions.map(q => q.module))];

module.exports = questions;
